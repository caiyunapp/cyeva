[project]
name = "cyeva-docs"
version = "0.2.0"
requires-python = ">=3.11, <3.13"

[tool.uv]
dev-dependencies = [
    "furo>=2024.8.6",
    "sphinx-rtd-theme>=3.0.1",
    "sphinx>=8.1.3",
]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
